# JDBC Student Management Application

A console-based Java application built with Maven that establishes a CRUD management lifecycle connection to a local MySQL server using JDBC.

## Features
- View all student profiles from a MySQL database
- Dynamically insert new student records via Java console UI
- Automated build path injection through Maven configuration
